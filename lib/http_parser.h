#include <stdbool.h>
    
struct HttpParser {

};

struct HttpRequest {

};

inline bool parse(struct HttpRequest* http_request, char request[]) {
    // Parse the request and put it into http_request
    return false;
}

